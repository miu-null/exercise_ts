import {Color} from "../types";

const myColor = Color.Red;

console.log(myColor);

const midori: Color = Color.Green;
console.log(midori);

console.log("-----------------");
const faveColor: Color = "Red" as Color;

console.log(faveColor);
