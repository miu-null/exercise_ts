export enum Color {
    Red = "Red",
    Green = "Green",
    Blue = "Blue",
}

export enum Direction {
    up = "Up",
    down = "Down",
    left = "Left",
    right = "Right",
}

export interface thingsInLife {
    colorOfChair: Color;
    arrow: Direction;
}
